# Build dependencies for the api-client-python GitBook docs.
#
# Used by scripts/build_api_client_python_docs.py to render the Sphinx +
# MyST sources in submodules/api-client-python/docs/source into the GitBook
# space at spaces/api-client-python/docs.
#
# Versions follow the Sphinx pipeline declared in
# submodules/api-client-python/pyproject.toml.
#
# Versions are pinned exactly so the verify_python_api_client_docs CI gate
# is deterministic: any change in autodoc / intersphinx output between
# local rebuilds and CI would otherwise show up as a spurious diff. To
# bump a dependency, change the pin here and rerun
# `make docker-docs-python-client` so the regenerated output uses the
# same toolchain CI does.

sphinx==7.4.7
myst-parser[linkify]==3.0.1
sphinx-markdown-builder==0.6.7
sphinx-design==0.6.1
sphinx-copybutton==0.5.2
pydata-sphinx-theme==0.16.1
