# Project Instructions

This is a GitHub repository of Markdown files that make up Talos Trading's data product documentation. Files are rendered via Gitbook. The repo includes a test suite that checks for formatting issues, broken links, and code errors.

## Rules

- **Never run `git commit` unless explicitly asked.** Always leave committing to the user so they can review changes first.
